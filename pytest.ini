[pytest]
markers =
    integration: MATLAB-reference integration tests (require collected values)
    matlab_parity: example parity tests comparing Python output against MATLAB .mat references
filterwarnings =
    ignore:The interactive_bk attribute was deprecated.*:matplotlib._api.deprecation.MatplotlibDeprecationWarning
    # Add any other warning filters you might need below
